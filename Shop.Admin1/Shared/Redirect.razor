@inject NavigationManager Navigation

@code {
    [Parameter] public string Url { get; set; } = null!;

    protected override async Task OnInitializedAsync() => Navigation.NavigateTo("/login", true);
}